= simple_form_for @deck, :html => { :class => 'form-horizontal' } do |f|
  = f.input :user_id
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), decks_path, :class => 'btn'
