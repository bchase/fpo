= simple_form_for [@deck, @card], :html => { :class => 'form-horizontal' } do |f|

  = f.input :front, input_html: { value: @line }
  = f.input :back

  .form-actions
    = f.button :submit, :class => 'btn btn-primary'
    -# = link_to t('.cancel', :default => t("helpers.links.cancel")), cards_path, :class => 'btn'
